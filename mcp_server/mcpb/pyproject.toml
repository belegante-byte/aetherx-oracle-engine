[project]
name = "aetherx-mcp-bundle"
version = "0.1.1"
description = "MCP bundle for the Aether-X Port Congestion Oracle"
requires-python = ">=3.10"
dependencies = [
    "mcp>=2.0.0,<3",
    "httpx>=0.24.0",
]
